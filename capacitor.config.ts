import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Control Pool',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
